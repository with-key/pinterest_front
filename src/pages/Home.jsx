import React from 'react';

// components
import Template from '../components/Template';

const Home = ({ history }) => {
	// console.log(history);
	return (
		<React.Fragment>
			<Template>Home</Template>;
		</React.Fragment>
	)
};

export default Home;
